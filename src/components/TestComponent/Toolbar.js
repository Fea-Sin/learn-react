import React from 'react';
import ThemeButton from '@/components/TestComponent/ThemeButton';

const Toolbar = (props) => {
  return (
    <div>
      <div>Toolbar is here</div>
      <ThemeButton />
    </div>
  )
}

export default Toolbar;